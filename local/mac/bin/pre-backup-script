#!/bin/zsh

assert_ok() {
    if [[ $? -ne 0 ]]; then
        echo "command $* failed, code $?"
        exit 1
    fi
}

### dump local installations
# homebrew
(cd /Users/whitley/Archive/backup; sudo -u whitley brew bundle dump)
assert_ok brew bundle dump
