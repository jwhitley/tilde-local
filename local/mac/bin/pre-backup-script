#!/bin/zsh

assert_ok() {
    if [[ $? -ne 0 ]]; then
        echo "command $* failed, code $?"
        exit 1
    fi
}

### dump local installations
# homebrew
assert_ok sudo -u whitley brew export > /Users/whitley/Archive/backup/homebrew-installed
